apiVersion: batch/v1
kind: CronJob
metadata:
  name: crawler-locations
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: service
            env:
            - name: DATABASE_HOST
              value: prod-primary-postgres