apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jwtks-service
spec:
  dnsNames:
  - $(SERVICE_NAME)
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-issuer
  secretName: jwtks-service-tls
