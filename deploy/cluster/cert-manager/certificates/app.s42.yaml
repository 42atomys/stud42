apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: s42-app
  namespace: istio-system
spec:
  dnsNames:
    - s42.app
    - "*.s42.app"
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  issuerRef:
    kind: ClusterIssuer
    name: ovh-issuer
  secretName: s42-app-tls
